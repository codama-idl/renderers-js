/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    type AccountMeta,
    type Address,
    type Instruction,
    type InstructionWithAccounts,
    type ReadonlyAccount,
    type WritableAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/kit/program-client-core';
import { DUMMY_PROGRAM_ADDRESS } from '../programs';

export type Instruction11Instruction<
    TProgram extends string = typeof DUMMY_PROGRAM_ADDRESS,
    TAccountOptionalAccount extends string | AccountMeta<string> = string,
    TAccountRequiredAccount extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithAccounts<
        [
            TAccountOptionalAccount extends string ? ReadonlyAccount<TAccountOptionalAccount> : TAccountOptionalAccount,
            TAccountRequiredAccount extends string ? WritableAccount<TAccountRequiredAccount> : TAccountRequiredAccount,
            ...TRemainingAccounts,
        ]
    >;

export type Instruction11Input<
    TAccountOptionalAccount extends string = string,
    TAccountRequiredAccount extends string = string,
> = {
    optionalAccount?: Address<TAccountOptionalAccount>;
    requiredAccount: Address<TAccountRequiredAccount>;
};

export function getInstruction11Instruction<
    TAccountOptionalAccount extends string,
    TAccountRequiredAccount extends string,
    TProgramAddress extends Address = typeof DUMMY_PROGRAM_ADDRESS,
>(
    input: Instruction11Input<TAccountOptionalAccount, TAccountRequiredAccount>,
    config?: { programAddress?: TProgramAddress },
): Instruction11Instruction<TProgramAddress, TAccountOptionalAccount, TAccountRequiredAccount> {
    // Program address.
    const programAddress = config?.programAddress ?? DUMMY_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        optionalAccount: { value: input.optionalAccount ?? null, isWritable: false },
        requiredAccount: { value: input.requiredAccount ?? null, isWritable: true },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('optionalAccount', accounts.optionalAccount),
            getAccountMeta('requiredAccount', accounts.requiredAccount),
        ],
        programAddress,
    } as Instruction11Instruction<TProgramAddress, TAccountOptionalAccount, TAccountRequiredAccount>);
}

export type ParsedInstruction11Instruction<
    TProgram extends string = typeof DUMMY_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        optionalAccount?: TAccountMetas[0] | undefined;
        requiredAccount: TAccountMetas[1];
    };
};

export function parseInstruction11Instruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas>,
): ParsedInstruction11Instruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 2) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 2,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    const getNextOptionalAccount = () => {
        const accountMeta = getNextAccount();
        return accountMeta.address === DUMMY_PROGRAM_ADDRESS ? undefined : accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { optionalAccount: getNextOptionalAccount(), requiredAccount: getNextAccount() },
    };
}
