/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
    combineCodec,
    getStructDecoder,
    getStructEncoder,
    getU8Decoder,
    getU8Encoder,
    SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
    SolanaError,
    type AccountMeta,
    type AccountSignerMeta,
    type Address,
    type FixedSizeCodec,
    type FixedSizeDecoder,
    type FixedSizeEncoder,
    type Instruction,
    type InstructionWithAccounts,
    type InstructionWithData,
    type ReadonlyAccount,
    type ReadonlySignerAccount,
    type ReadonlyUint8Array,
    type WritableAccount,
} from '@solana/kit';
import {
    getAccountMetaFactory,
    getResolvedInstructionAccountAsProgramDerivedAddress,
    type InstructionAccountInput,
    type InstructionAccountInputAddress,
    type InstructionSignerInput,
    type ResolvedInstructionAccount,
    type ResolvedInstructionAccountMeta,
} from '@solana/kit/program-client-core';
import { DUMMY_PROGRAM_ADDRESS } from '../programs';

export type Instruction12Instruction<
    TProgram extends string = typeof DUMMY_PROGRAM_ADDRESS,
    TAccountAuthority extends string | AccountMeta<string> = string,
    TAccountDelegate extends string | AccountMeta<string> = string,
    TAccountTarget extends string | AccountMeta<string> = string,
    TAccountMetadata extends string | AccountMeta<string> = string,
    TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
    InstructionWithData<ReadonlyUint8Array> &
    InstructionWithAccounts<
        [
            TAccountAuthority extends string
                ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
                : TAccountAuthority,
            TAccountDelegate extends string ? ReadonlyAccount<TAccountDelegate> : TAccountDelegate,
            TAccountTarget extends string ? WritableAccount<TAccountTarget> : TAccountTarget,
            TAccountMetadata extends string ? ReadonlyAccount<TAccountMetadata> : TAccountMetadata,
            ...TRemainingAccounts,
        ]
    >;

export type Instruction12InstructionData = { bump: number };

export type Instruction12InstructionDataArgs = Instruction12InstructionData;

export function getInstruction12InstructionDataEncoder(): FixedSizeEncoder<Instruction12InstructionDataArgs> {
    return getStructEncoder([['bump', getU8Encoder()]]);
}

export function getInstruction12InstructionDataDecoder(): FixedSizeDecoder<Instruction12InstructionData> {
    return getStructDecoder([['bump', getU8Decoder()]]);
}

export function getInstruction12InstructionDataCodec(): FixedSizeCodec<
    Instruction12InstructionDataArgs,
    Instruction12InstructionData
> {
    return combineCodec(getInstruction12InstructionDataEncoder(), getInstruction12InstructionDataDecoder());
}

export type Instruction12Input<
    TAccountAuthority extends InstructionSignerInput = InstructionSignerInput,
    TAccountDelegate extends InstructionAccountInput | InstructionSignerInput =
        InstructionAccountInput | InstructionSignerInput,
    TAccountTarget extends InstructionAccountInput = InstructionAccountInput,
    TAccountMetadata extends InstructionAccountInput = InstructionAccountInput,
> = {
    authority: TAccountAuthority;
    delegate: TAccountDelegate;
    target: TAccountTarget;
    metadata: TAccountMetadata;
    bump?: Instruction12InstructionDataArgs['bump'];
};

export function getInstruction12Instruction<
    TAccountAuthority extends InstructionSignerInput,
    TAccountDelegate extends InstructionAccountInput | InstructionSignerInput,
    TAccountTarget extends InstructionAccountInput,
    TAccountMetadata extends InstructionAccountInput,
    TProgramAddress extends Address = typeof DUMMY_PROGRAM_ADDRESS,
>(
    input: Instruction12Input<TAccountAuthority, TAccountDelegate, TAccountTarget, TAccountMetadata>,
    config?: { programAddress?: TProgramAddress },
): Instruction12Instruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<TAccountAuthority, InstructionAccountInputAddress<TAccountAuthority>>,
    ResolvedInstructionAccountMeta<
        TAccountDelegate,
        InstructionAccountInputAddress<TAccountDelegate>,
        ReadonlySignerAccount<InstructionAccountInputAddress<TAccountDelegate>> &
            AccountSignerMeta<InstructionAccountInputAddress<TAccountDelegate>>
    >,
    ResolvedInstructionAccountMeta<TAccountTarget, InstructionAccountInputAddress<TAccountTarget>>,
    ResolvedInstructionAccountMeta<TAccountMetadata, InstructionAccountInputAddress<TAccountMetadata>>
> {
    // Program address.
    const programAddress = config?.programAddress ?? DUMMY_PROGRAM_ADDRESS;

    // Original accounts.
    const originalAccounts = {
        authority: { value: input.authority ?? null, isSigner: true, isWritable: false },
        delegate: { value: input.delegate ?? null, isSigner: 'either', isWritable: false },
        target: { value: input.target ?? null, isSigner: false, isWritable: true },
        metadata: { value: input.metadata ?? null, isSigner: false, isWritable: false },
    };
    const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

    // Original args.
    const args = { ...input };

    // Resolve default values.
    if (!args.bump) {
        args.bump = getResolvedInstructionAccountAsProgramDerivedAddress('metadata', accounts.metadata.value)[1];
    }

    const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
    return Object.freeze({
        accounts: [
            getAccountMeta('authority', accounts.authority),
            getAccountMeta('delegate', accounts.delegate),
            getAccountMeta('target', accounts.target),
            getAccountMeta('metadata', accounts.metadata),
        ],
        data: getInstruction12InstructionDataEncoder().encode(args as Instruction12InstructionDataArgs),
        programAddress,
    } as Instruction12Instruction<
        TProgramAddress,
        ResolvedInstructionAccountMeta<TAccountAuthority, InstructionAccountInputAddress<TAccountAuthority>>,
        ResolvedInstructionAccountMeta<
            TAccountDelegate,
            InstructionAccountInputAddress<TAccountDelegate>,
            ReadonlySignerAccount<InstructionAccountInputAddress<TAccountDelegate>> &
                AccountSignerMeta<InstructionAccountInputAddress<TAccountDelegate>>
        >,
        ResolvedInstructionAccountMeta<TAccountTarget, InstructionAccountInputAddress<TAccountTarget>>,
        ResolvedInstructionAccountMeta<TAccountMetadata, InstructionAccountInputAddress<TAccountMetadata>>
    >);
}

export type ParsedInstruction12Instruction<
    TProgram extends string = typeof DUMMY_PROGRAM_ADDRESS,
    TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
    programAddress: Address<TProgram>;
    accounts: {
        authority: TAccountMetas[0];
        delegate: TAccountMetas[1];
        target: TAccountMetas[2];
        metadata: TAccountMetas[3];
    };
    data: Instruction12InstructionData;
};

export function parseInstruction12Instruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
    instruction: Instruction<TProgram> &
        InstructionWithAccounts<TAccountMetas> &
        InstructionWithData<ReadonlyUint8Array>,
): ParsedInstruction12Instruction<TProgram, TAccountMetas> {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 4,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            authority: getNextAccount(),
            delegate: getNextAccount(),
            target: getNextAccount(),
            metadata: getNextAccount(),
        },
        data: getInstruction12InstructionDataDecoder().decode(instruction.data),
    };
}
